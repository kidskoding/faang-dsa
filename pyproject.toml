[project]
name = "faang-dsa"
version = "0.1.0"
description = "dsa prep in Python for internship, co-op, and new-grad interviews at big tech companies"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "mdformat>=0.7.22",
    "mdformat-frontmatter>=2.0.8",
    "mdformat-gfm>=0.4.1",
    "ruff>=0.11.13",
    "ty>=0.0.1a1",
]

[tool.mdformat]
wrap = 88

[tool.pytest.ini_options]
pythonpath = [
    "redos/2026-08-25",
    "redos/2026-08-24",
    "01_arrays_and_hashing",
    "02_two_pointers",
    "03_stacks_and_queues",
    "04_sliding_window",
    "05_binary_search",
    "06_linked_lists",
    "07_trees",
    "08_heaps",
    "09_backtracking",
    "10_graphs",
    "11_dp",
    "12_greedy_algorithms",
    "13_intervals",
    "14_tries",
    "15_bit_manipulation",
    "16_math_geometry",
    "17_advanced",
    "18_mixed_interview_practice",
    "redos/2026-08-23",
]
testpaths = [
    "01_arrays_and_hashing",
    "02_two_pointers",
    "03_stacks_and_queues",
    "04_sliding_window",
    "05_binary_search",
    "06_linked_lists",
    "07_trees",
    "08_heaps",
    "09_backtracking",
    "10_graphs",
    "11_dp",
    "12_greedy_algorithms",
    "13_intervals",
    "14_tries",
    "15_bit_manipulation",
    "16_math_geometry",
    "17_advanced",
    "18_mixed_interview_practice",
    "redos"
]

[tool.ruff]
line-length = 130
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ty.environment]
extra-paths = [
    "01_arrays_and_hashing",
    "02_two_pointers",
    "03_stacks_and_queues",
    "04_sliding_window",
    "05_binary_search",
    "06_linked_lists",
    "07_trees",
    "08_heaps",
    "09_backtracking",
    "10_graphs",
    "11_dp",
    "12_greedy_algorithms",
    "13_intervals",
    "14_tries",
    "15_bit_manipulation",
    "16_math_geometry",
    "17_advanced",
    "18_mixed_interview_practice",
]

[tool.ty.rules]
empty-body = "ignore"

[[tool.ty.overrides]]
# 800 stubs that get solved, rewritten, and re-solved. Gating CI on
# half-finished practice code would turn the build red most evenings.
include = ["**/problem_set/**"]

[tool.ty.overrides.rules]
no-matching-overload = "ignore"

[[tool.ty.overrides]]
# Tests build a known-good structure and then walk it, so node.next.next is
# safe even where the annotation says the link is optional. Asserting the shape
# first would only pad the test. Solutions get no such pass: an unchecked
# optional there is a real bug, and one ty already caught in the tree code.
include = ["**/tests/**"]

[tool.ty.overrides.rules]
unresolved-attribute = "ignore"
invalid-argument-type = "ignore"
not-subscriptable = "ignore"
invalid-assignment = "ignore"
