# Prerequisite Ledger

What each note **establishes**. A note may assume everything above it and must
not re-teach it. When you need a prior idea, link it in one line and move on:

```md
You met Dijkstra in [10_graphs/07](../../10_graphs/notes/07_weighted_shortest_paths.md).
Prim differs from it by exactly one line.
```

Readers go through the repo in module order, so "above" means lower module
number, or lower note number inside the same module.

Update this file whenever a note is added, renamed, or changes what it covers.

## 00_fundamentals

| Note                         | Establishes                                                                                                                                                |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_how_to_prep               | Learn → focused practice → specific review → later cold re-solve → mixed practice; OA/live/mock progression                                                |
| 02_python_basics             | Interview Python; functions/type hints; iteration; mutation/aliasing; `list`/`dict`/`set`/tuple; slicing; sorting; comprehensions; `heapq`; `deque`        |
| 03_time_and_space_complexity | Big-O derivation; named input variables; sequential/nested/halving work; worst case; output vs auxiliary space; amortized analysis                         |
| 04_common_operation_costs    | Documented list/dict/set/deque/heap/string/sort costs; average vs worst vs amortized bounds; choosing by the repeated operation                            |
| 05_interview_problem_solving | Natural clarify → example → brute force → bottleneck → optimize → code/test narration → analyze conversation; invariants, debugging, hints, and follow-ups |

Every later note may assume Big-O notation and Python fluency without comment.

## 01_arrays_and_hashing

| Note                    | Establishes                                                                                                                                                                                           |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_dynamic_arrays       | Indexed contiguous storage; size vs capacity; geometric growth and amortized append; in-place mutation, swaps, swap-delete, range-to-index marking, matrix boundaries, and three-reversal rotation    |
| 02_hashing              | Set membership vs map state; seen-set and complement lookup; frequency maps; signatures and grouping; sequence heads; length-prefix encoding; two-candidate Boyer-Moore cancellation and verification |
| 03_prefix_suffix_sums   | Leading-zero prefix arrays and O(1) range sums; running split totals; prefix/suffix products; prefix-frequency and earliest-index maps; equal-remainder ranges; 2D inclusion-exclusion                |
| 04_kadanes_algorithm    | Local extend-or-restart state vs global optimum; all-negative initialization; boundary recovery; running-min, product, circular, absolute, turbulent, and empty-allowed repeated-array variations     |
| 05_hash_table_internals | Hash functions, buckets, equality, collisions, separate chaining vs open addressing, duplicate-key updates, load factor, resizing/rehashing, and average/worst/amortized bounds                       |

## 02_two_pointers

| Note                            | Establishes                                                                                                                                                                                                                       |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_opposite_end_pointers        | Forced converging movement; alphanumeric and backspace skipping; fixed-value pair scans; duplicate and multiplicity counts; subset counts from valid min/max bounds; extreme pairing; water boundaries; token-score peak tracking |
| 02_same_direction_pointers      | Read/write cursors with `write <= read` at iteration start; in-place filtering and swapping; backward writes; forward-region counting formulas; Floyd cycle-entry alignment; three-way and virtual-index partitioning             |
| 03_in_place_mutation (absorbed) | Swapping, backward writing, stale-tail semantics, and three-way partitioning are covered by `02_same_direction_pointers`; no standalone note exists                                                                               |

## 03_stacks_and_queues

| Note                          | Establishes                                                                                                                                                                                  |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_stack                      | LIFO and Python `list` operations; matching; nested-state parsing; value/index/tuple entry choices; expression evaluation and stack simulation                                               |
| 02_queue_and_deque            | FIFO vs LIFO; `collections.deque`; list-front cost; ring buffers; queue-from-stacks amortization; recent-event windows; two-deque middle access                                              |
| 03_monotonic_stack            | Ordered unresolved candidates; next/previous greater/smaller; strict equality policy; span boundaries, sentinels, contributions, and one-push/one-pop amortization                           |
| 04_monotonic_queue (absorbed) | `04_sliding_window/04_window_max_min` owns monotonic-deque domination, index expiry, fixed/variable/coordinate windows, prefix-sum deques, and window recurrences; no standalone note exists |

## 04_sliding_window

| Note                     | Establishes                                                                                                                                                                                                                                                  |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 01_fixed_size_window     | Fixed vs variable width; prime/add/drop updates; sum, predicate-count, and set state; fixed base plus window gain; contiguous-complement windows                                                                                                             |
| 02_variable_size_window  | One-direction validity requirement; longest-invalid-shrink vs shortest-valid-shrink; at-most counting; budget/cost windows; last-seen jumps and stale positions; sorted raise-cost and bit-mask variants                                                     |
| 03_frequency_map_windows | Membership vs multiplicity; delete-at-zero distinct counts; fixed multiset matching; `have`/`need`/`formed` containment; `at_most(k) - at_most(k - 1)` exact counts; complement and word-unit maps; order-sensitive near-miss; stale maximum-frequency proof |
| 04_window_max_min        | **Monotonic deque** domination; index vs value entries; fixed max/min; fixed, left-pointer, and coordinate expiry; two-deque spread windows; negative-prefix shortest subarray; max-plus-sum state; fixed-bound last-position counting                       |

## 05_binary_search

| Note                    | Establishes                                                                                                                                                                                                                                                              |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 01_binary_search_basics | Inclusive exact search and insertion position; candidate-region invariant and termination; overflow-safe midpoint; comparison-oracle search; chained-row matrix flattening with `divmod`; independently sorted matrix staircase                                          |
| 02_boundary_search      | Half-open first-true and first-false boundaries with `n` sentinel; predicate monotonicity and post-loop validation; `bisect_left`/`bisect_right` equality semantics; timestamp, derived-count, fixed-window-edge, per-query, peak-slope, and mountain-array applications |
| 03_rotated_arrays       | Two sorted runs and rotation point; sorted-half target containment; right-end minimum search; zero-rotation behavior; duplicate ambiguity and linear worst case; mountain local-slope search                                                                             |
| 04_search_on_answer     | Inclusive minimum- and maximum-feasible value search; monotonicity and endpoint proofs; integer-safe midpoint and ceiling division; greedy grouping/adjacency predicates; counting predicates for kth values; shorter-array median partition                             |

## 06_linked_lists

| Note                  | Establishes                                                                                                   |
| --------------------- | ------------------------------------------------------------------------------------------------------------- |
| 01_linked_list_basics | `ListNode`, pointer walking, dummy head                                                                       |
| 02_fast_slow          | Cycle detection, midpoint, nth-from-end                                                                       |
| 03_reversal           | Three-pointer flip, recursive flip, sublist and k-group reversal, reversal as a front/back pairing subroutine |
| 04_merge_split        | Merging sorted lists, splitting in halves                                                                     |

## 07_trees

| Note             | Establishes                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 01_fundamentals  | `TreeNode` and `None` children; root/parent/child/leaf/edge/subtree; depth vs height and the nodes-vs-edges convention; perfect/complete/balanced/skewed shapes and `h` between `log2 n` and `n`; why a single-pointer loop fails and the call stack is the pending-branch bookkeeping; the return contract, `None` base cases, and recurse-then-combine; paired two-tree recursion; shape shortcuts as in `count_nodes` |
| 02_dfs           | Pre/in/post order, top-down vs bottom-up helpers, path backtracking                                                                                                                                                                                                                                                                                                                                                      |
| 03_bfs           | Level-order with a queue; frozen `level_size` loops; per-level payloads (right side view, averages, zigzag, `next` links); early-exit minimum depth; tuple queue entries carrying parent, position index, or coordinates; parent-map BFS from a non-root node with a visited set                                                                                                                                         |
| 04_path_problems | Root-to-leaf and any-path accumulation                                                                                                                                                                                                                                                                                                                                                                                   |
| 05_bst           | Ordering invariant, inorder is sorted, bounded search                                                                                                                                                                                                                                                                                                                                                                    |
| 06_construction  | Root-plus-split recursion; preorder ambiguity; inorder as the splitter; value-to-index maps and index ranges instead of slices; one shared cursor consumed front-to-back or back-to-front; value windows for BST from preorder; range-maximum and midpoint roots                                                                                                                                                         |
| 07_serialization | Round-trip contract; why values alone are ambiguous; null markers and self-delimiting `2n + 1` token counts; preorder encode/decode through one shared iterator; iterative level-order format with `None` in the queue and truncated input; serialization as a hash/substring key with leading separators; list-append vs `+=` string building                                                                           |
| 08_complexity    | Height vs node count, balanced vs skewed cost                                                                                                                                                                                                                                                                                                                                                                            |

Recursion depth, call-stack space `O(h)`, and "recurse then combine" are assumed
from here on.

## 08_heaps

| Note           | Establishes                                                                                                                                                                                                                                                                                                                                                     |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_heap_basics | Min-heap, `heapq`, O(log n) push/pop, tuple ordering, max-heap via negation                                                                                                                                                                                                                                                                                     |
| 02_top_k       | Size-`k` capped heap whose root is the admission cutoff; min-heap for k largest and negated max-heap for k smallest; kth-largest as the root; heap order is not sorted order; count-then-select over `d` distinct values; inverted `__lt__` wrapper and `nsmallest` keys for opposite-direction tie-breaks; uncapped pop-modify-push loops over changing counts |
| 03_two_heaps   | Facing heaps around a rank boundary (low max-heap / high min-heap); ordering vs size invariants; push-and-relay placement; **lazy deletion** with a delayed counter and tracked live sizes; pipeline heaps where a monotone gate heap feeds a ranking heap                                                                                                      |
| 04_k_way_merge | One candidate per source in the heap; `(value, source_index, position)` entries and the source-index tie-break; empty-source filtering and exhausted-source termination; stopping after `k` pops; generated successors with a **seen set**; extra state beside the heap such as a running maximum or a covered point; `O(N log k)` versus `O(N log N)`          |

Heaps are assumed known in graphs and advanced modules — never re-explain
`heapq`.

## 09_backtracking

| Note                    | Establishes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_backtracking_basics  | Decision tree over partial solutions and why it is never allocated; generate-then-filter and why testing the prefix replaces it; **pruning** checked before the recursive call, including bounds on what a branch can still achieve; choose / explore / un-choose as `append`/`pop`, `True`/`False`, `add`/`discard`; what a missing un-choose does when parameters unwind but shared state does not; copying the path at save time; choices from a lookup table and a branching factor that varies by character; the three base-case shapes (collect a copy, `total += 1`, `best = max(...)`) and count-only searches with no `path`; the state / choices / complete / prune questions |
| 02_subsets_combinations | Include-exclude, start index to avoid duplicates                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 03_permutations         | Used-set / swap approaches                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 04_grid_backtracking    | Shared mutable state instead of a `path` list; permanent vs per-path visited marks; sentinel written into the board and restored on the single exit path; base case before the bounds check; boolean short-circuit vs counting searches; constraint sets for row/column/box and for column, `r - c`, `r + c` diagonals; undo as a physical action (turn, move, turn); bucket-assignment boards with overshoot pruning, descending order, and the empty-bucket symmetry break                                                                                                                                                                                                            |

## 10_graphs

| Note                           | Establishes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_graph_basics                | Node/edge/neighbor/degree, path, cycle, connected component; directed vs undirected, weighted edges, self-loops and parallel edges; edge list as the usual input and why rescanning it is `O(V * E)`; building an adjacency list in `O(V + E)` with the two `append` lines for undirected and a `defaultdict(list)` for string or sparse labels; adjacency list vs matrix tradeoff and when a matrix is handed to you; visited set as the thing trees did not need, marked on commit (before recursing, at push or enqueue) rather than at pop; recursive vs iterative DFS and the 1000-frame recursion limit; BFS giving fewest edges with the `dist` map doubling as the visited set; `len(visited) == n` as the reachability answer; adjacency lists in disguise (`rooms[i]`, employee ids indexed into a dict); original-node-to-clone map for Clone Graph                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 02_grid_dfs                    | Grids as **implicit graphs** with no adjacency list built and a `DIRECTIONS` tuple computing neighbours on demand; why a cell-plus-up-left local test undercounts a bent island; flood fill and the mark that must actually change the cell; sinking the input grid as a free visited set versus a `(row, column)` set when the caller's data must survive; the permanent mark contrasted with grid backtracking's restore, and the wrong answer the restore produces; bounds test before indexing, with `IndexError` off the bottom/right and silent negative-index wraparound off the top/left; checks in the callee versus at the call site when the rule compares child to parent; specialising the helper by its return value with the rejected call as the identity element (area `0`, perimeter `1` plus a third marker, "holds everywhere" `True`); never short-circuiting a sink behind `and`/`all`; inverting enclosed-region questions by seeding from the four borders, and returning `False` off-board versus `True` on water when regions must be counted individually; labelling islands with ids from `2` and a size map instead of erasing them, with distinct neighbour ids and a `default=0` seed; explicit-stack grid DFS marking on push                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 03_grid_bfs                    | Grids as unweighted graphs and why DFS returns a non-shortest first arrival; wavefront expansion and the first-arrival-is-final argument from non-decreasing pop order; marking visited at enqueue rather than at pop; the three homes for a step count (queue tuple, parallel distance grid, per-level counter) and never two at once; **multi-source BFS** seeded before the first pop, justified as a **virtual source** with cost-zero edges, with a distance grid whose sentinel doubles as the visited set; eight-direction moves and counting cells instead of moves; stopping on a condition such as a border cell rather than a fixed goal; DFS flood fill to paint a region then BFS to measure between regions (Shortest Bridge, `return steps` not `steps + 1`); grid cycle detection carrying the parent cell in the queue entry with a shared `visited` across restarts; unreachability signalled by an empty queue plus a counter such as `fresh`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 04_components_cycles_bipartite | Outer loop plus one shared `visited` set; component counting and `O(V²)` adjacency-matrix sweeps; undirected cycle detection with the parent skip and its parallel-edge caveat; directed white/gray/black DFS; tree checks via `V - 1` edges plus one component, and incremental reachability for the redundant edge; BFS 2-coloring, forced colors, the odd-cycle theorem, and conflict graphs; doubled edges carrying a direction flag; zero-in-degree starts; functional-graph walks; bridges via `disc`/`low`; reverse-time component rebuilding; components over non-integer nodes such as emails                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 05_topological_sort            | Topological order as a shape-only ordering and its non-uniqueness; DAGs; the `O(V * (V + E))` rescan and why stored readiness replaces it; in-degree, sources, and Kahn's algorithm; seeding over `range(n)`, enqueuing on exactly zero, and flipping prerequisite pairs to get the edge direction; `len(order) < V` as the cycle proof, plus reversed-graph Kahn for eventual safe states; queue vs stack vs heap for tie-breaking; DFS reverse postorder with `UNSEEN`/`ACTIVE`/`DONE`, the `DONE` short-circuit, the recursion-depth ceiling, and finish-order reuse in Hierholzer itineraries; undirected leaf peeling to tree centres; computing along the order for node-weighted longest paths, ancestor sets, and per-node counter arrays; implicit DAGs solved by memoized DFS instead of built in-degrees                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 06_implicit_state_bfs          | **Implicit graphs** whose nodes are **states** and edges are **transitions**, given as start state plus transition rule plus goal test instead of an adjacency list; "minimum number of moves" over non-obvious nodes as the signal, and the three things it is not (weighted moves, count/list-all, unbounded state space); hashable states (tuple not list, `frozenset` not `set`) and complete states, with the step count deliberately kept in the queue entry and out of the key; seeding the `seen` set with forbidden states plus the separate start-is-forbidden guard; generating neighbours by string surgery (`s[:i] + ch + s[i + 1:]`, wheel wraparound with `% 10`) instead of materialising an `O(N² * L)` adjacency list; a collected-item **bitmask** with \`mask                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 07_weighted_shortest_paths     | Edge weights, cost of a path, and why "fewest edges" and "cheapest total" come apart; first arrival is not cheapest arrival, so BFS's mark-on-sight is the move that must go; subdividing every weight-`w` edge into `w` unit edges as a correct but `O(V + W)` repair, and keeping only the visit order it implies; **Dijkstra** with a `dist` table, a min-heap of `(cost, node)` with cost leading, settling on pop, and **relaxation**; the four load-bearing lines (tuple order, `if d > dist[u]: continue` **lazy deletion** of stale entries, strict `<` improvement test, `dist[v]` written at push time); the exchange argument for why the first pop is final and where it uses nonnegativity, hence Bellman-Ford for negative edges; reading an answer off the whole `dist` array (`max`, `math.inf` as the unreachable sentinel, 1-indexed nodes with an `n + 1` adjacency list); swapping only the combine step, with `max(d, w)` for **minimax / bottleneck** paths, `d * p` with negated probabilities for reliability (`best` seeded at `0.0`, source at `1.0`, every comparison reversed), and the monotonicity condition that combining must never make a path cheaper; the early `return` when the target is popped; the near-miss `nd = w` that is Prim's MST rather than a shortest path; consistent-ratio graphs (*Evaluate Division*) needing DFS with a running product and no heap at all; **0-1 BFS** with a `deque`, `appendleft` on a 0-edge and `append` on a 1-edge, `O(V + E)` because only two distinct distances are ever queued; **state augmentation** keying `dist` by `(node, extra)` such as `(city, flights used)`, comparing against the exact state rather than the node, and the `V * k` state-space blowup |

Graph vocabulary and Dijkstra are settled here. Module 17 must not redefine
them.

## 11_dp

| Note               | Establishes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_dp_fundamentals | DP as recursion with a notebook; the vocabulary of **state**, **transition**, **base case**, and the `dp` table; **overlapping subproblems** (with merge sort as the counterexample) and **optimal substructure** (with longest simple path as the counterexample); the exponential call count of naive Climbing Stairs and its cure; **memoization** as a cache check after the base cases and a store before returning, plus `functools.cache` with its hashable-argument and nested-helper caveats, and the 1000-frame recursion ceiling; the four-line state/transition/base/answer plan and the "`dp[...]` is the ..." sentence test; "using the first `i` items" (`dp[n]`) vs "ending exactly at index `i`" (`max(dp)`), House Robber vs LIS; enlarging an under-informed state (max/min pair, holding flag) and the catalogue of table shapes `dp[i]`, `dp[r][c]`, `dp[i][cap]`, `dp[i][j]`, `dp[i][flag]`; **tabulation** and defending the fill order in one and two dimensions; rolling variables for `O(1)` space and losing reconstruction; cost as states × work per transition, plus rejecting a `2^n` state design |
| 02_1d_dp           | Single-index state on a line vs a prefix-sum aggregate; why a fixed alternating pattern fails; the best-up-to-`i` vs best-ending-at-`i` contract and whether the answer is `dp[n-1]`, `dp[n]`, or `max(dp)`; guarded reach-back and silent negative-index wrap; counting base case `1` vs maximisation `0`; `n + 1` tables for reach-position states; collapsing to `one_back`/`two_back` and the assignment order; re-indexing onto a derived axis (Delete and Earn points line over the full value range); removing a circular constraint by two linear runs (House Robber II)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 03_2d_grid_dp      | Pair-indexed state and `dp[r][c]` as an answer distinct from the input grid; path enumeration costing `C(m + n - 2, m - 1)` versus `m * n` states; last-move case split, disjoint-and-exhaustive combines, and add-vs-`min` on the same table; dependency arrows deciding row-major fill order; base row/column as 1s, running totals, or a padded border whose identity is `0` for a sum and `float("inf")` for a `min`; obstacles written as blocking zeros instead of skipped cells; answers spread over a whole row (falling path) instead of one corner; base-case position choosing bottom-up fill (Triangle) to remove per-row edge cases; rolling one or two rows for `O(n)` space and the in-place-safety rule that a leftward read must be the current row's, with a saved `prev_diag` when it is not; anchoring a shape to its bottom-right corner (`1 + min` of three neighbours) and summing that table to count all squares                                                                                                                                                                                         |
| 04_knapsack        | Capacity as a second state dimension (`dp[i][cap]`) and why the item index alone is under-informed; the value-per-weight greedy counterexample; skip/take recursion on `(index, room)` and why remaining room replaces the chosen-item set; the item-by-capacity grid; collapsing to one row and the **sweep direction** rule (0/1 downward, unbounded upward) with the reuse bug the wrong direction causes; `range(capacity, weight - 1, -1)` and negative-index wraparound; `max` → `+` for counting with `dp[0] = 1`; items-outer counts combinations vs totals-outer counts ordered sequences (Coin Change II vs Combination Sum IV); boolean reachability subset sums with a parity reject (Partition Equal Subset Sum); two simultaneous budgets as a grid (Ones and Zeroes); the `P = (total + target) / 2` reframing (Target Sum); **pseudo-polynomial** `O(n * C)` bounds                                                                                                                                                                                                                                               |
| 05_sequence_dp     | Subsequence vs substring and why no window describes a subsequence; the two-prefix rectangle `dp[i][j]` and the pair of positions as the state; no local rule for which side to skip; LCS with diagonal-on-match and `max` of two on mismatch; backward walk to reconstruct the string and what rolling to two rows costs; same rectangle with `+` (Distinct Subsequences), boolean `or` and the implied `i + j` third index (Interleaving String), and `min` over three neighbours (Edit Distance); wildcard and regex tables where `*` reads a same-row neighbour and row 0 is filled separately; one-index states scanning backwards (Word Break, Longest Valid Parentheses) and the `tails` + `bisect_left` `O(n log n)` LIS; **span** states `dp[i][j]` over `s[i..j]` filled bottom-up for palindromes and layered under Palindrome Partitioning II; **interval DP** splitting on the *last* move with sentinel padding and increasing-span fill (Burst Balloons); index-plus-mode running values as a state machine and the required simultaneous tuple assignment (stock with cooldown or fee)                            |

## 12_greedy_algorithms

| Note                   | Establishes                                              |
| ---------------------- | -------------------------------------------------------- |
| 01_greedy_fundamentals | Local choice, exchange argument, when greedy fails vs DP |
| 02_jump_game           | Reachability frontier                                    |
| 03_interval_greedy     | Sort-by-end scheduling                                   |

The exchange argument is established here — the cut property in MST can lean on
it.

## 13_intervals

| Note               | Establishes                                   |
| ------------------ | --------------------------------------------- |
| 01_interval_basics | Overlap test, sorting by start vs end         |
| 02_merge_insert    | Merging overlaps, inserting into a sorted set |
| 03_meeting_rooms   | Room counting with a heap                     |
| 04_sweep_line      | Delta events, running active count            |

## 14_tries

| Note               | Establishes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_trie_basics     | Prefix tree nodes, insert/search, terminal flags                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 02_word_dictionary | Queries naming a set of paths rather than one; `.` wildcards turning the trie walk into a DFS over `node.children.values()` with `any` short-circuiting; the node-plus-pattern-index state and the index-equals-depth invariant that makes backtracking free and caps a search at the trie size; base case returning `is_word` rather than `True`, with `search("ca")` as the catching assert; a **budget** of mismatches threaded through the same recursion, `budget == 0 and node.is_word` for "exactly one change" versus "at most one"; transforming the keys instead of the search (reversed words for a stream, `suffix + "#" + word` keys, node payloads for ranking); collecting the whole subtree below a prefix node via `sorted(node.children)` for lexicographic order, appending a node's own word before descending, and cutting off at the required count |
| 03_trie_plus_dfs   | Trie-guided grid/word search                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## 15_bit_manipulation

| Note              | Establishes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_bitwise_basics | AND/OR/XOR/shift, two's complement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 02_masks          | Set/clear/test a bit, mask building                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 03_xor_patterns   | Self-cancellation, missing/duplicate tricks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 04_subset_masks   | A **subset mask** as one integer standing in for a subset over a fixed item list, with bit `i` meaning item `i` is in; why an integer beats a `frozenset` key (`mask \| (1 << i)`, `a & b == a` containment, `dp[mask]` indexing a flat list with no hashing); `for mask in range(1 << n)` as the enumeration replacing `n` nested loops, `mask.bit_count()` as subset size, mask `0` as the empty subset and `subsets([]) == [[]]`, and increasing numeric order visiting every subset before its supersets; the `2^20` / `2^25` ceiling and a tiny cap on `n` as the signal; reading an array **sideways** one bit column at a time with `ones * (n - ones)` disagreeing pairs (Total Hamming Distance); building an answer greedily from the top bit down with a prefix set and `candidate ^ p` probes, a rejected bit settled at 0 forever (Maximum XOR of Two Numbers); **submask enumeration** with `sub = (sub - 1) & mask` from `sub = mask`, emitting `0` before the break, `O(3^n)` across all masks versus `O(4^n)` filtering, and each split generated twice; a mask as a memo **index** for `dp[mask]`, and one round per bucket scoring a split as `max(dp[mask ^ sub], total[sub])` (Fair Distribution of Cookies) |

## 16_math_geometry

| Note                  | Establishes                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_matrix_coordinates | Index transforms, rotation, spiral order                                                                                                                                                                                                                                                                                                                                                          |
| 02_modular_arithmetic | Mod rules, overflow-safe accumulation                                                                                                                                                                                                                                                                                                                                                             |
| 03_gcd_lcm            | Divisors/multiples, coprime, Euclid, lcm identity, Bezout                                                                                                                                                                                                                                                                                                                                         |
| 04_geometry_basics    | `(x, y)` points vs `(row, col)`; squared Euclidean distance instead of `sqrt`; Manhattan distance; gcd-reduced sign-normalized direction pair as a line key; cross product for turn direction, collinearity, and shoelace triangle area; convex hull by Andrew's monotone chain; axis-aligned rectangles as two intervals, overlap test, inclusion-exclusion area, and corner-parity tiling check |

Manhattan distance is defined here — MST's point-connection variant can assume
it.

## 17_advanced

| Note                  | Establishes                                                                                                                     |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 01_union_find         | Disjoint sets, `find`/`union`, path compression, union by rank, "same component?" in ~O(1)                                      |
| 02_shortest_paths     | Bellman-Ford, Floyd-Warshall, negative weights                                                                                  |
| 03_mst                | **Cut property, Kruskal, Prim**                                                                                                 |
| 04_range_structures   | Segment tree, Fenwick tree, range query/update                                                                                  |
| 05_string_algorithms  | KMP, Z algorithm, Rabin-Karp                                                                                                    |
| 06_divide_and_conquer | Recurrence splitting, merge-sort-shaped counting                                                                                |
| 07_bitmask_dp         | Subset states as DP dimensions                                                                                                  |
| 08_bloom_filters      | Probabilistic membership, one-sided error, `k` hashes by double hashing, `m`/`k` sizing formulas, counting variant, union by OR |

## 18_mixed_interview_practice

| Note               | Establishes                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01_timed_solving   | Time boxes as invariants pointed at the clock; why one deadline at the end gives no actionable reading; the checkpoint schedule (contract 3, brute force named 8, commit 14, running code 27, hand trace 32, complexity 35) and how to scale it; the commit rule and narrating the drop to the slower approach; picking a slow scaffold the fast version edits in one expression; stating complexity on whatever currently runs |
| 02_pattern_review  | Recognizing which pattern a blind problem wants                                                                                                                                                                                                                                                                                                                                                                                 |
| 03_mock_interviews | Full-loop simulation                                                                                                                                                                                                                                                                                                                                                                                                            |

These assume the entire book and teach no new technique.
